# Build System

This folder defines the operating process for building GTM/FDE automation loops from messy ideas into tested, approval-ready n8n workflows and Open WebUI agents.

Use `build-process.md` as the master workflow and `build-status-template.md` as the standard status format.


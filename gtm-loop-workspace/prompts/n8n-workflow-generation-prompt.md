# n8n Workflow Generation Prompt

Convert this loop spec into an n8n implementation plan.

Return trigger node, validation node, enrichment nodes, AI nodes, branch nodes, approval gate, write/action nodes, log node, error path, credentials, env vars, test payloads, and import checklist.

Loop spec:

```text
<paste loop spec>
```

